import React from 'react';
import GridLayout from '../components/GridLayout';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <GridLayout />
    </div>
  );
};

export default Home;
