import React from 'react';
import GridLayoutChat from '../components/GridLayout';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <GridLayoutChat />
    </div>
  );
};

export default Home;
